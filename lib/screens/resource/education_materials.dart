import 'package:flutter/material.dart';

class EducationMaterialsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Education Materials')),
      body: Center(child: Text('Content for Education Materials')),
    );
  }
}
